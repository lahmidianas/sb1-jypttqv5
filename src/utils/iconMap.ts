import { Building, Users, Award, Home, Key, Settings, MapPin, Phone, Mail, Clock } from 'lucide-react';
import { IconMap } from '../types/home';

export const iconMap: IconMap = {
  Building,
  Users,
  Award,
  Home,
  Key,
  Settings,
  MapPin,
  Phone,
  Mail,
  Clock
};